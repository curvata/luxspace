<form method="post" action="{{ path('departure.delete', {'id': departure.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ departure.id) }}">
    <button class="btn_delete">Supprimer</button>
</form>
